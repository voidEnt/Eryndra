# MAP-004 Pass 03 stock tile delta

Everything in Pass 02's dependency manifest remains, except the wall record `(11,5)` now uses stock `Inside_C.png` tile ID `329` instead of stock B tile `91`. At 48px the chart shows columns and rows with illegible marks, no world geography, names or measurements. The separate desk record stays stock B tile `91`; the corrected south wall remains stock A4 tile `7048` (`0xE0F`). The review project must have stock Inside tileset ID 3 containing B and C sheets. No custom asset, plugin, dialogue or new gameplay dependency.
